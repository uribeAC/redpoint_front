import type React from "react";
import "./Header.css";

const Header: React.FC = () => {
  return (
    <header className="main-header">
      <div className="main-header_container">
        <h1 className="main-header_title">🔴Redpointt</h1>
      </div>
    </header>
  );
};

export default Header;
